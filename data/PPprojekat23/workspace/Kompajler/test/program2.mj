program p
int c, d[], e;
char a[], aa;
bool gggg;
const int size = 10, width = 50, length = 100; //komentar1
const bool b = true;
const char karakter = 'b';
const int width = 30;
const int nova = 34;
char c;
class Table {
	int pos[], neg[], factor;
    {
		//Table(int factor) {this.factor = factor;}
		void putp (int a, int idx) { this.pos[idx] = a; } 
		void putn (int a, int idx) { this.neg[idx] = a; } 
		int getp (int idx) { return pos[idx]; }
		int getn (int idx) { return neg[idx]; }
	} 
}

class GlassBottle extends Bottle{
	int ne, bi, smelo;
}

class Klasa {
	{
		void putp (int a, int idx) int b;{ this.pos[idx] = a; } 
		void putn (int a, int idx) { this.neg[idx] = a; } 
		int getp (int idx) { return pos[idx]; }
		int getn (int idx) { return neg[idx]; }
		//Table met() { return null; }
	}
}

class GlassBottle extends Table{
	char ovo, bi, moralo;
}

{
	int main()
		char niz[];
	{
	return 1;
	}
}		